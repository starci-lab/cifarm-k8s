apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: cifarm-server-postgresql-master-persistent-volume-claim
  namespace: default
spec:
  storageClassName: cifarm-server-postgresql-master-storage-class
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi