# Secret configuration
secret:
  metadata:
    name: websocket-node-build-secret
  imageCredentials:
    registry: "https://index.docker.io/v1/"
    username: ""
    password: ""
    email: ""

# Kaniko configuration
kaniko:
  metadata:
    name: websocket-node-kaniko
  args:
    image:
      repository: cifarm/websocket-node
      tag: latest
    dockerfile: ./apps/websocket-node/Dockerfile
    context: git://github.com/starci-lab/cifarm-containers
  resources: 
    requests:
      cpu: "1"
      memory: "1Gi"
    limits:
      cpu: "2"
      memory: "2Gi"

