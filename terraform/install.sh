# All the runtime that are required for the terraform to work are installed here.
sudo apt-get update

# The uuid-runtime package is installed to generate a unique identifier for the plan file.
sudo apt-get install uuid-runtime
# The dos2unix package is installed to convert the line endings of the scripts to Unix format.
sudo apt-get install dos2unix


